# GRQM lockfile (2026-05-21 prune/coherence pass)
#
# Intent:
# - direct dependencies pinned to the declared project contract where possible
# - transitive dependencies pinned to a coherent resolved set for reproducibility
# - this file is a governed execution artifact, not merely a reflection of whatever
#   happened to be installed in one sandbox
#
# Direct dependencies from pyproject.toml / requirements*.txt:
numpy==2.3.3
scipy==1.17.0
astropy==6.1.7
sympy==1.13.0
pytest==8.4.2

# Transitive/runtime support dependencies:
iniconfig==2.1.0
packaging==25.0
pluggy==1.6.0
pyerfa==2.0.1.5
PyYAML==6.0.3
mpmath==1.3.0
astropy-iers-data==0.2025.5.19.0.38.36
